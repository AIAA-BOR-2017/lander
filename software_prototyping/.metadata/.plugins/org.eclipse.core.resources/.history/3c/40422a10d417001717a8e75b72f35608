/*
 * lander_os.c
 *
 *  Created on: Apr 2, 2017
 *      Author: zane
 */

cmd pop(cmd* head){
    head = (head++) & (BIT2|BIT1|BIT0); // increment and wrap to 0-7
    return &head;
}

cmd push(cmd data, cmd* tail){
    tail = (tail++) & (BIT2|BIT1|BIT0); // increment and wrap to 0-7
}
